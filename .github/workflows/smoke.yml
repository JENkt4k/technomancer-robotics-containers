name: Smoke

on:
  push:
  pull_request:

jobs:
  compose-and-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate compose files and demo scaffold
        run: bash scripts/test-smoke.sh
